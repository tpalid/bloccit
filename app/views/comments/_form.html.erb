<%= form_for [post, post.comments.new] do |f| %>
    <div class='form-group'>
        <%= f.label :comment %></br>
        <%= f.text_area :body %>
    </div>
    <div class="form-group">
        <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
<% end %>

    